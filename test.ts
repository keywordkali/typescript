let nbr = 0; //pure javascript
let nbr2: number = 1; //how to decare variable in typescript
let str: string = "abc"; //declaring string in typescript
//let str2: string = 0; //<< This is incorrect and will show an error because 0 is not a string.

function add1(n: number): number {
    return n + 1;
}
